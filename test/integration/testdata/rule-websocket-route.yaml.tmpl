type: route-rule
name: websocket-route
namespace: {{.Namespace}}
spec:
  destination:
    name: {{.Destination}}
  precedence: 6
  match:
    request:
      headers:
        testwebsocket:
          exact: enabled
  route:
    - labels:
         version: v1
  websocketUpgrade: true
